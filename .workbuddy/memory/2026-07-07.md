# 2026-07-07

## 项目专业分析（EquipSense / EquipAI）
- 受用户委托对 EquipSense 做了专业分析，已写入 `output/project-analysis-2026-07-07.md` 并附架构图。
- 实测结论：后端 `dotnet build` **0 错误 0 警告**（net8.0，本地 .NET 10 SDK）；410 个 .cs、约 3.5 万行手写业务代码；前端 192 个 ts/tsx；i18n 中英文各 915 键对齐。
- 测试：后端 938 个 xUnit 用例（112 单元 + 26 集成文件），前端 41 单测文件，Playwright E2E 52 spec（位于 `frontend/e2e-comprehensive/`）。
- 文档极详尽：`FINAL_TECHNICAL_DESIGN.md` 2457 行 + `docs/evaluation/` 33 文件 6689 行。
- 综合评级 8.7/10（A-）。核心闭环（采集→告警→AI根因→工单）实现扎实。
- 关键风险：① 生产用 `EnsureCreated` 无增量迁移/回滚（R1 高）；② 告警聚合器为进程内内存态、多副本不协同（R2 中高）；③ 种子默认弱口令 Admin@123 等（R3 中）；④ 文档体量过大漂移风险（R4 中）；⑤ 前端测试偏 hooks、页面级仅 1 个（R5 中）。
- 代码亮点：`Program.cs` 启动期 fail-fast 校验（JWT 弱密钥/CORS）、`api.ts` 401 刷新队列+工业弱网处理、`AlertEvaluationService` 处理重启重置/阈值震荡边界。

## 低风险加固（用户选定，4 项）
- 新增 `global.json`：`sdk.version=8.0.100` + `rollForward=latestMajor`。本地仅 10.0.201 → 解析到 10.0.201；CI 用 8.0.x → 用 8.0.x。两侧都不破坏，构建仍 0 错误 0 警告。（注：不能写死 10.0.201，否则与 CI 的 DOTNET_VERSION=8.0.x 冲突）
- `Program.cs` 4 处 OTel 的 `Console.WriteLine` → `Log.Information`（走 Serilog，结构化参数 `{Endpoint}`）。另 5 处在 `OpcUaMockServer`（Simulator 控制台程序），Console 即其天然日志出口，故意保留。
- E2E 纳入 PR 门禁：`.github/workflows/ci.yml` 的 `e2e` job `if` 增加 `|| github.event_name == 'pull_request'`（`docker` job 保持仅 main 推送）。`99-manual-audit` 已被 playwright config 的 projects 排除（无匹配项目），不会自动跑。
- 种子口令（R3）复核：已是标准软强制——`SEED_*_PASSWORD` 覆盖 + `MustChangePassword=true` + 登录返回标记供前端引导改密 + 用默认口令打警告。**无需改代码**（硬拦截会破坏 E2E）。
- 后续建议（未做，留待生产化阶段）：R1 数据库迁移策略(Migrations)、R2 告警聚合迁 Redis、登录硬拦截未改密用户（需先确保 E2E 不依赖默认口令登录）。
