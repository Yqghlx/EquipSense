# 蓝绿部署 Nginx upstream — 由 scripts/deploy-bluegreen.sh 自动维护
# 手动修改后需 docker exec equipai-router nginx -s reload 生效
# 首次部署默认指向 blue；首次启用流程见 docs/BLUE_GREEN_DEPLOY.md
upstream backend_active { server backend-blue:8080; }
upstream frontend_active { server frontend-blue:80; }
