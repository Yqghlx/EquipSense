# EquipSense 技术债务与改进路线图

> 分析日期：2026-07-02（数据校正） · 来源：全部 14 份评估报告的待改进项汇总 · 总计 32 项

---

## 一、来源追溯矩阵

每项改进均可追溯标记：

| 标记 | 来源报告 |
|------|---------|
| 🏗 | 02-后端架构 |
| 🎨 | 03-前端架构 |
| ⚙ | 04-边缘网关 |
| 📐 | 05-代码质量 |
| 🔒 | 06-安全纵深 |
| 📊 | 07-运维 |
| 🔄 | 08-DevOps |
| 🗄 | 09-数据库 |
| 📡 | 10-API |
| ⚡ | 11-性能 |
| 📦 | 12-依赖 |
| 🧪 | 14-测试 |

---

## 二、P0 — 立即处理（本周）

| # | 项 | 来源 | 影响 | 目标时间 | 方案 |
|---|-----|------|------|---------|------|
| 1 | **清理 602MB tmux 日志（本地磁盘）** | 📐 | 本地磁盘占用（注：该文件未被 git 跟踪，`*.log` 已在 `.gitignore`，不存在仓库膨胀/信息泄露） | 立刻 | 删除本地 `tmux-server-*.log`；本项非紧急安全问题 |
| 2 | ~~**修复 NuGet CVE** (NU1901/1903)~~ | 📦 | 生产运行含已知漏洞 | 2 天 | ✅ **已完成**（`dotnet list package --vulnerable` 全绿，移除 `NoWarn NU1901/NU1903` 抑制） |
| 3 | ~~**MQTT 传输未加密** 🔒~~ | 🔒 ⚙ | 遥测数据工厂内网明文传输 | 2 天 | ✅ **已完成**（后端 `MqttOptions.UseTls` + `MqttSecurityConfigurationValidator` 生产强制 TLS；边缘网关对称配置；mosquitto.prod.conf 8883） |
| 4 | ~~**后台服务分布式锁**~~ | 🏗 📊 | 多实例 10 个后台任务重复执行 | 3 天 | ✅ **已完成**（`IDistributedLockProvider` + `RedisDistributedLockProvider`（SET NX PX + Lua 解锁）；`LockedTimerService` 基类；8 个周期性服务迁移，MQTT 长连接订阅者正确排除） |

---

## 三、P1 — 短期内处理（1-2 周）

| # | 项 | 来源 | 影响 | 方案 |
|---|-----|------|------|------|
| 5 | ~~**InMemoryEventBus 扩容**~~ | 🏗 ⚡ | Channel 1000 满丢事件 | ✅ **已完成**（容量 1000→10000；FullMode 改 `Wait` + 5s 超时兜底，业务事件不再静默丢弃） |
| 6 | ~~**工单编码并发冲突**~~ | 🏗 | 并发创建时可能产生重复编码 | ✅ **已完成**（抽取 `WorkOrderCodeGenerator` 共享重试逻辑，统一手工建单 + 自动建单两条路径；`IgnoreQueryFilters` 跨租户读最大序号 + 唯一约束冲突重试 3 次） |
| 7 | ~~**Trivy 改为阻断 CI**~~ | 🔄 | 已知 CVE 不阻止部署 | ✅ **已完成**（`exit-code: '1'` + `severity: HIGH,CRITICAL` + `ignore-unfixed: true`） |
| 8 | ~~**K6 压测集成 CI**~~ | 🔄 ⚡ | 无法检测性能退化 | ✅ **已完成**（CI 新增 `load-test` job：k6 跑 `api-read.js` 读路径冒烟，20 VU × 30s，P95<500ms/错误率<1% 阈值内建，超阈值 k6 退出码非零阻断 CI；指标导出 JSON 上传 artifact 便于跨版本对比） |
| 9 | ~~**添加 CD 自动部署**~~ | 🔄 | 镜像推送后手动 deploy | ✅ **已完成**（CI 新增 `deploy` job：tag 发布后 SSH 到生产服务器，`docker-compose.prod.yml` 覆盖拉取 GHCR 镜像（不在服务器本地编译），滚动重启 backend/frontend；30s 等待 + 120s 健康门禁轮询 `/health`，失败自动回滚到 `.last-deployed-tag`；GHCR 凭证保留服务器本地不通过 CI 传输；未配 `DEPLOY_SSH_KEY` 时 job 自动跳过不影响 fork） |
| 10 | ~~**语义版本号**~~ | 🔄 | `:latest`+`:sha` 无法区分版本 | ✅ **已完成**（CI 新增 `release` job：tag 推送 `vX.Y.Z` 触发，`docker/metadata-action@v5` 从 tag 推导 semver 标签 `1.2.3`/`1.2`/`1`/`latest`；`softprops/action-gh-release` 自动生成 GitHub Release；WebAPI.csproj `<Version>` 同步至 `1.2.0`） |
| 11 | **TelemetryService 同步阻塞修复** | 📐 ⚡ | `GetAwaiter.GetResult()` 阻塞线程 | ✅ **已完成**（`DisposeAsync` 异步主路径 + 同步 `Dispose` 兜底 `Wait(10s)` 超时；边缘网关 7 天缓冲） |
| 12 | ~~**Jaeger/Tempo 容器**~~ | 📊 | OTel 只能 Console 输出 | ✅ **已完成**（`jaegertracing/all-in-one:1.62.0`：16686 UI + 4317/4318 OTLP，`COLLECTOR_OTLP_ENABLED=true`，后端 OTLP gRPC 推送 + Console fallback，安全加固 no-new-privileges/cap_drop） |
| 13 | ~~**边缘网关 API 强制 HTTPS**~~ | ⚙ 🔒 | AuthKey HTTP 明文传输 | ✅ **已完成**（`GatewayOptions` 新增 `RequireHttps`；`Program.cs` 启动门禁：开启后 `BackendUrl` 非 https 立即抛 `InvalidOperationException` 阻止启动，防止 `X-Gateway-Auth-Key` 头明文泄露；默认 `false` 兼容 Docker Compose 内网 HTTP，独立网关跨网络部署时设 `Gateway__RequireHttps=true`；已记入 environment-variables.md） |
| 14 | ~~**NuGet 漏洞不再静默**~~ | 📦 | 删除 NU1901/NU1903 抑制 | ✅ **已完成/无需处理**（`Directory.Build.props` 仅抑制 `NU1603`（版本解析提示，良性）；`NU1901`/`NU1903`（漏洞警告）从未被抑制，且 `TreatWarningsAsErrors=true` 使其直接阻断构建；CI 额外跑 `dotnet list package --vulnerable --include-transitive` 做二次门禁。当前全项目 0 漏洞依赖） |
| 15 | ~~**Prometheus 告警规则文件**~~ | 📊 | 只有路由配置，无告警规则 | ✅ **已完成**（`docker/prometheus/rules.yml`：18 条规则分 4 组 — service-availability / http-health / process-resources / backend-health，覆盖 BackendDown/MqttDisconnected/HighErrorRate/HighLatency/CriticalAlertsSurge/WorkOrderBacklog 等；AlertManager 路由已配） |

---

## 四、P2 — 中期（1 个月）

| # | 项 | 来源 | 方案 |
|---|-----|------|------|
| 16 | ~~**SettingsPage.tsx (1,578 行) 拆分**~~ | 📐 🎨 | ✅ **已完成**（commit `d1ff4e4`：拆分为 248 行 + `components/settings/` 子组件，包括 MfaSettingsPanel/SubscriptionPanel/ApprovalChainSettings/SystemInfoCard/UserManagementPanel/IntegrationSettings/NotificationPreferenceCard） |
| 17 | ~~**KnowledgeController.cs (858 行) 拆分**~~ | 📐 🏗 | ✅ **已完成**（拆为 `Controllers/Knowledge/` 下 4 文件：`KnowledgeRulesController.cs`（351 行，规则 CRUD + 冲突检测 + 导入导出 + 预置 + 版本/回滚）、`PendingRulesController.cs`（273 行，候选规则审核工作流）、`FaultCasesController.cs`（68 行，故障案例查询）、`KnowledgeDtos.cs`（244 行，共享 DTO + `KnowledgeMapper`）；路由前缀 `api/v1/knowledge` 保持不变，前端零改动；7 个集成测试 + 1024 单元测试全绿） |
| 18 | ~~**DeviceImportService.cs (678 行) 拆分**~~ | 📐 🏗 | ✅ **已完成**（拆为 `partial class` 4 文件：`DeviceImportService.cs`（139 行，公共 API 协调）、`DeviceImportService.Persist.cs`（107 行，事务内配额+去重+批量写入+计数修正）、`DeviceImportService.Parsing.cs`（321 行，CSV/JSON 解析+字段提取）、`DeviceImportService.Validation.cs`（150 行，行级校验+location 规范化+枚举/日期解析）；公共 API 零变更，27 单元测试 + 103 集成测试全绿） |
| 19 | ~~**CSV 下载重复模式封装**~~ | 📐 🎨 | ✅ **已完成**（commit `30daa23`：抽取 `frontend/src/lib/utils.ts` 的 `downloadBlob` 工具函数，7 处 hook 复用，非 `useFileDownload` 而是 `downloadBlob`） |
| 20 | ~~**前端组件测试补充**~~ | 📐 🧪 | ✅ **已完成**（测试文件 41 → 42，测试用例 321 → 342（+21），覆盖率提升：lines 81.2% → 85.6%（+4.4pp）、functions 83.6% → 84.7%、statements 79.5% → 83.6%、branches 67.5% → 76.8%。重点补强两处薄弱点：① `usePermission.test.tsx` 4 → 17 用例，补全 MaintenanceLead/Operator/Viewer/未知角色 + useUserRole 覆盖（原仅 SystemAdmin/Technician，覆盖率 62% → ~100%）；② 新增 `lib/__tests__/api.test.ts`（8 用例），覆盖响应拦截器安全关键路径——401 刷新重试、登录/刷新请求 401 防无限循环（DoS 防护）、已重试 _retry 防循环、未登录跳转登录页、刷新失败跳转。附带 `api.ts` 重构：动态 import → 静态 import（notificationStore/authStore 仅依赖 zustand + 类型无循环依赖，静态导入消除异步开销且利于 tree-shaking）。友好错误提示 toast 因 vi.mock('axios') 模块图隔离无法跨图断言，留由 E2E 覆盖（e2e-comprehensive/）） |
| 21 | ~~**集成测试补充**~~ | 🧪 | ✅ **已完成**（新增 14 个 Controller 集成测试，覆盖 DeviceTypes/Notifications/AuditLogs/Dashboard/Reports/DataQuality/Gateways/DeviceConfig/Fmea/TrendAnalysis/DeviceComparison/Evaluation/WorkOrderAttachments/GatewayConfig/Sla；集成测试 103 → 152（+49），15/15 Controller 全覆盖；每个测试覆盖 auth 拦截（401）+ happy path + 参数校验/边界 404。修复附带发现的 PushNotificationService 构造函数 bug：VAPID 未配置时构造抛 InvalidOperationException → ExceptionHandlingMiddleware 映射 409 → 拉取本服务的 SLA 端点全部失败；改为延迟降级（构造存储 null，发送时检查并记 Warning 跳过），SlaController 现可用） |
| 22 | ~~**Controller 直接注入 DbContext**~~ | 📐 | ✅ **已完成**（11 个 Controller 全部解耦 AppDbContext：Analyses/DeviceTypes/Dispatch/AlertRules/Notifications/DeviceConfig/WorkOrderAttachments/Alerts/Gateways/Integration/GatewayConfig；新增 11 个 Application 层服务封装查询与编排：；DTO 随之下沉到 Application 层；统一 CancellationToken 参数模式——ASP.NET Core 自动绑定 RequestAborted 且支持单元测试无 HTTP 上下文调用；适配 3 个既有单元测试 + 1138 测试全绿（1035 单元 + 103 集成）；`grep -l "private readonly AppDbContext" Controllers/*.cs` 归零） |
| 23 | ~~**Dependabot 配置**~~ | 🔄 📦 | 自动化依赖更新 PR | ✅ **已完成**（`.github/dependabot.yml`：4 生态全覆盖 — github-actions/nuget/npm/docker，每周一集中出 PR；按生态分组（React 同步升级、EF Core/Npgsql 整组、测试工具整组）减少 PR 数；npm major 版本忽略走人工评估；配合 CI 的 NuGet 漏洞阻断 + npm 审计 + Trivy 扫描形成闭环） |
| 24 | ~~**代码覆盖率阈值**~~ | 🧪 | ✅ **已完成**（前后端覆盖率采集 + 阈值门禁，ratchet 棘轮机制只升不降。**后端**：`Tests/coverage.runsettings` 配置 coverlet 排除 EF 迁移 + 源生成器产物；CI unit + integration 分别采集 → `reportgenerator` 合并 → `scripts/check-coverage.mjs` 按程序集门禁（Core 80% / Application 78% / WebAPI 50% / Edge 28%；Infrastructure + Simulator 豁免——前者迁移类已排除、剩余仓储受集成测试波动大，后者非生产代码）。基线：Core 85%、Application 81%、WebAPI 56%、Edge 32%、Infra 79%（迁移排除后）；门禁回归测试 exit 1 验证通过。**前端**：`vitest.config.ts` 配置 v8 coverage + thresholds（lines 80% / functions 80%），基线 lines 81.2% / functions 83.6%；新增 `@vitest/coverage-v8` devDep；排除 test 夹具/类型定义/main.tsx。覆盖率报告作为 artifact 上传（14 天保留）便于跨版本回归对比） |
| 25 | ~~**OPC UA 生产安全模式告警**~~ | 🔒 ⚙ | ✅ **已完成**（`EquipAI.Core/Security/OpcUaSecurityConfigurationValidator.cs`：生产环境检测 None → 返回 Error 告警（明文可被嗅探/篡改），Sign → Warning（仅签名可被嗅探），SignAndEncrypt → 无告警；**告警不阻断**因部分老旧 PLC 不支持安全模式，由宿主 `Log.Error/Warning` 记录；校验器返回 `(Level, Message)?` 元组避免在 Core 层引入日志包依赖；`enabledProtocols` 过滤——OPC UA 未启用时跳过校验；边缘网关 `Program.cs` 集成，11 单元测试覆盖大小写/空值/协议过滤/级别分支） |

---

## 五、P3 — 长期（3 个月+）

| # | 项 | 来源 | 方案 |
|---|-----|------|------|
| 26 | **事件总线迁移 RabbitMQ** | 🏗 | 提供持久化 + 重试 + 死信 |
| 27 | **CQRS + 只读副本** | 🏗 ⚡ | 复杂查询分离到只读库 |
| 28 | ~~**SAST 扫描集成**~~ | 🔒 🔄 | ✅ **已完成**（`.github/workflows/codeql.yml`：GitHub CodeQL 语义级 SAST，选 CodeQL 而非 SonarCloud 因公开仓库免费且无需 SONAR_TOKEN secret。覆盖 C# 后端 + TypeScript/JavaScript 前端（matrix 按语言独立分析）；查询集 security-extended（比默认更全：含路径遍历、SSRF、日志注入、硬编码凭证、不安全反序列化）；触发：main/develop push + PR + 每周一 03:17 全量兜底；C# autobuild 注入 .NET 8 SDK 编译 EquipAI.slnx，TS/JS autobuild 无需构建；SARIF 上传 GitHub Security tab（PR 内联标注 + main 基线追踪）。与 CI 现有安全门禁互补：Gitleaks（密钥泄露）+ dotnet --vulnerable（NuGet CVE）+ npm 审计 + Trivy（镜像 CVE）+ CodeQL（源码 0-day 模式），形成纵深防御） |
| 29 | ~~**蓝绿/灰度部署**~~ | 🔄 📊 | ✅ **已完成**（零停机蓝绿部署方案，作为默认滚动部署的可选升级路径。新增 4 文件：`docker/docker-compose.bluegreen.yml`（backend/frontend blue/green 双实例 + router 服务，profiles 隔离，端口 8081/8082 + 3001/3002）、`docker/nginx.bluegreen.conf`（router Nginx 配置，upstream_active 通过 include 热切换）、`docker/upstream-active.conf`（活跃色 upstream，部署脚本维护）、`scripts/deploy-bluegreen.sh`（编排脚本：读活跃色→目标色拉镜像启动→健康门禁→Nginx reload 原子切换<1s→drain 30s 停旧色→记录）。流程：blue 服务时部署 green，健康检查通过后 Nginx reload 切流量，旧 blue drain 后停止。失败处理：健康检查不过不切换旧色继续服务；切换后异常可秒级回切 upstream。新增 `docs/BLUE_GREEN_DEPLOY.md`（架构图 + 首次启用 + 常规部署 + 失败回切 + 资源考量对比表）。资源代价：双实例 +1.3G 内存换零停机，资源紧张时保留默认滚动部署。脚本 bash -n + compose YAML 校验通过） |
| 30 | **混沌工程** | 📊 | Chaos Mesh 故障注入 |
| 31 | ~~**P99 延迟 SLO 定义**~~ | 📊 | ✅ **已完成**（`docs/SLO.md`：5 项 SLI + 5 项 SLO + 错误预算。SLO-1 API 可用性 ≥99.9%（28 天 ≤40min 宕机）；SLO-2 读路径 P95<500ms/P99<1000ms；SLO-3 写路径 P95<1000ms/P99<2000ms；SLO-4 MQTT 丢失<0.01%；SLO-5 实时推送 P95<2s。目标值基于 PERF_BASELINE.md 实测（读 P99 50ms，写 P95 40ms）留 10-25x 余量。错误预算机制：预算耗尽冻结新功能发布。k6 阈值同步升级为机器可执行契约：standardThresholds 增 P99<1000ms + 错误率收紧至 0.1%，relaxedThresholds 增 P99<2000ms，CI load-test job 每次合并自动执行。含观测告警分级（Warning/Critical）与季度评审机制） |
| 32 | **E2E 用例优化精简** | 🧪 | 从 512 精简到 270 消除重复 |

---

## 六、改进项分布统计

### 按优先级

| 优先级 | 数量 | 预计工作量 |
|--------|------|-----------|
| P0 (本周) | 4 | 3-5 天 |
| P1 (1-2 周) | 11 | 10-15 天 |
| P2 (1 个月) | 10 | 15-20 天 |
| P3 (3 月+) | 7 | 25-50 天 |
| **合计** | **32** | **55-90 天** |

### 按领域

| 领域 | P0 | P1 | P2 | P3 | 合计 |
|------|----|----|----|----|------|
| 🔒 安全 | 1 | 1 | 1 | 1 | 4 |
| 📐 代码质量 | 1 | 1 | 5 | — | 7 |
| 🏗 后端架构 | 1 | 2 | — | 2 | 5 |
| 🎨 前端 | — | — | 2 | — | 2 |
| ⚙ 边缘网关 | 1 | 1 | 1 | — | 3 |
| 📊 运维 | 1 | 2 | — | 2 | 5 |
| 🔄 DevOps | — | 3 | 1 | 1 | 5 |
| ⚡ 性能 | — | 1 | — | 1 | 2 |
| 📦 依赖 | 1 | 1 | 1 | — | 3 |
| 🧪 测试 | — | — | 2 | 1 | 3 |

---

## 七、实施建议路径

### 第一周 (P0)
```
① 清理本地 tmux 日志（删除 tmux-server-*.log；注：未入 git，*.log 已在 .gitignore，非仓库膨胀问题）
② 修复 NuGet CVE (逐包升级)
③ MQTT TLS (MqttClientOptionsBuilder)
④ Redis 分布式锁 (RedLock)
```

### 第二-三周 (P1)
```
⑤ InMemoryEventBus 扩容 (1000→10000)
⑥ 工单编码修复 (DB sequence)
⑦ Trivy 阻断 (exit-code: 1)
⑧ K6 CI 集成
⑨ Sequential：Jaeger 容器 → CD 部署 → semver
```

### 第 4-8 周 (P2)
```
⑩ SettingsPage 拆分
⑪ KnowledgeController 拆分
⑫ DeviceImportService 拆分
⑬ CSV hook 封装
⑭ 组件测试补充
⑮ 集成测试补充
⑯ Controller DbContext 修复
⑰ Dependabot 配置
```

### 第 9 周+ (P3)
```
⑱ RabbitMQ 迁移
⑲ CQRS + 只读副本
⑳ SAST 扫描
㉑ 蓝绿部署
㉒ SLO 定义
```

---

## 关联报告

| 领域 | 报告 |
|------|------|
| 架构总览 | [S01-全系统架构总览图](./S01-全系统架构总览图.md) |
| 代码质量 | [05-代码质量分析](./05-代码质量分析.md) |
| 安全纵深 | [06-安全纵深分析](./06-安全纵深分析.md) |
| 运维缺口 | [07-运维与可观测性分析](./07-运维与可观测性分析.md) |
| DevOps 改进 | [08-DevOps与CI_CD分析](./08-DevOps与CI_CD分析.md) |
| 测试改进 | [14-测试策略与金字塔分析](./14-测试策略与金字塔分析.md) |
| Bug 演进 | [S06-Bug历史与演进故事](./S06-Bug历史与演进故事.md) |

---
*本文档属于 EquipSense 项目评估体系 · 生成日期：2026-06-24 · 版本：v3.1*
