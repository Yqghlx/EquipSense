# EquipSense 测试策略与金字塔分析报告

> 分析日期：2026-07-02（数据校正） · 范围：后端 138 文件 · 前端 41 文件 · E2E 52 spec · K6 7 脚本

---

## 一、测试全景

### 1.1 总量统计

| 层 | 技术栈 | 文件数 | 用例数 | 断言数 | 代码行 | 运行时间（估） |
|----|--------|--------|--------|--------|--------|--------------|
| **后端单元测试** | xUnit + FluentAssertions + Moq | 112 | 835 (807 Fact + 28 Theory) | ~1,821 | ~18,000 | ~30s |
| **后端集成测试** | xUnit + Testcontainers + WebApplicationFactory | 26 | 103 | ~600 | ~11,500 | ~120s |
| **前端单元测试** | Vitest + Testing Library + jsdom | 41 | 321 | ~758 | ~8,000 | ~30s |
| **前端 E2E** | Playwright (Chromium) | 52 spec | 512 | — | ~5,000 | ~30min |
| **压力测试** | K6 | 7 脚本 | — | — | ~1,000 | 手动执行 |
| **合计** | | **238** | **1,771+** | **~3,179** | **~43,500** | **~35min** |

### 1.2 测试框架对比

> 注：后端单元测试与集成测试**均使用 xUnit**（两个测试项目 `EquipAI.Tests.Unit.csproj` / `EquipAI.Tests.Integration.csproj` 均引用 `xunit 2.5.3`），不存在双框架混用问题。早期文档误记为 NUnit，已校正。

| 特性 | xUnit (后端单元+集成) | Vitest (前端) | Playwright (E2E) |
|------|-----------------|---------------|-----------------|
| 执行方式 | dotnet test | vitest run | playwright test |
| Mock 框架 | Moq | vi.mock | — |
| 断言 | FluentAssertions | expect | expect |
| 并行 | ✅ | ✅ | ❌ (fullyParallel=false) |
| 环境依赖 | 无 / Testcontainers (PG) | jsdom | 真实 PG + Redis |
| 持续运行 | — | vitest watch | playwright --ui |

---

## 二、测试金字塔现状

```
        ┌──────────────────────┐
        │  E2E: 512 (29%)      │  52 spec · 8 categories · 30min
        ├──────────────────────┤
        │  集成: 103 (6%)      │  26 files · Testcontainers · 120s
        ├──────────────────────┤
        │  前端单测: 321 (18%) │  41 files · Vitest · 30s
        ├──────────────────────┤
        │  后端单测: 835 (47%) │  112 files · xUnit · 30s
        └──────────────────────┘
```

### 2.1 问题诊断

| 问题 | 影响 | 建议目标 |
|------|------|---------|
| E2E 占比 29%（偏高） | 运行 30min，维护成本高，flaky | 降至 15%（~270 用例） |
| 集成测试仅 6%（偏低） | 仅覆盖 11/30 Controller | 升到 25%（覆盖全部 Controller） |
| 后端单测 47%（正常） | xUnit 统一框架，结构健康 | 保持 |
| 前端组件测试覆盖率低 | 50+ 组件仅 ~10 个有测试 | 达到 80% 组件覆盖率 |

### 2.2 理想目标金字塔

```
        ┌──────────────────────┐
        │  E2E: 200 (15%)      │  核心链路 + 安全测试
        ├──────────────────────┤
        │  集成: 300 (25%)     │  全部 30 Controller + Repository
        ├──────────────────────┤
        │  单测: 900 (60%)     │  服务 + 组件 + 工具
        └──────────────────────┘
```

---

## 三、后端单元测试详情

### 3.1 按功能域覆盖

| 功能域 | 测试文件数 | 断言数 | 覆盖内容 |
|--------|-----------|--------|---------|
| **中间件** | 8 | ~200 | WAF(4 类攻击检测), SecurityHeaders(9 个响应头), UsageLimit(配额), ExceptionHandling(异常映射) |
| **告警引擎** | 6 | ~180 | Threshold/Baseline/Combined 评估器, AlertAggregator(防风暴), TryAutoResolve(自动恢复) |
| **工单生命周期** | 5 | ~150 | 全 9 状态转换矩阵, 非法流转拒绝, 编码生成 |
| **多租户隔离** | 4 | ~80 | DeviceService FindAsync 跨租户隔离不变量, OutputCache 泄漏测试 |
| **数据分析** | 7 | ~100 | RootCauseAnalysisEngine(降级逻辑), DeviceHealthService(加权评分), OeeService(可用率×性能×质量) |
| **服务层** | 12 | ~200 | AuthService(登录/锁定/MFA/刷新), UserService(CRUD/角色), DeviceService(CRUD/导入) |
| **知识库** | 8 | ~120 | KnowledgeCaptureService(案例生成/规则批准), KnowledgeConflictService(冲突检测), RuleAccuracyTracker(准确率) |
| **边缘网关** | 5 | ~80 | Protocol 适配器(解析/连接), SqliteBufferStore(Store/GetPending/Cleanup) |
| **控制器** | 15 | ~400 | AuthController(登录/注册/刷新), DevicesController(CRUD/导入/导出), AlertsController(确认/解决) |
| **SignalR Hub** | 3 | ~50 | IndustrialHub(租户组连接/断开), SignalRNotificationService(多路推送) |

### 3.2 测试模式示例

```csharp
// WafMiddlewareTests — 白盒测试 + 端到端测试
public class WafMiddlewareTests
{
    [Fact]
    public void SqlInjection_ShouldBeDetected()
    {
        // 白盒: 静态方法直接测试正则
        WafMiddleware.IsMalicious("' OR 1=1 --").Should().BeTrue();
    }

    [Fact]
    public async Task NormalRequest_ShouldPass()
    {
        // 端到端: 完整中间件管线
        var context = new DefaultHttpContext();
        context.Request.Method = "GET";
        var middleware = new WafMiddleware(_ => Task.CompletedTask, Mock.Of<ILogger<WafMiddleware>>());
        await middleware.InvokeAsync(context);
        context.Response.StatusCode.Should().Be(200);
    }
}
```

---

## 四、后端集成测试

### 4.1 当前覆盖

| Controller | 集成测试 | 覆盖场景 |
|-----------|---------|---------|
| AuthController | ✅ | login / register / refresh / MFA-verify / MFA-setup |
| DevicesController | ✅ | CRUD / import / export / health-score |
| AlertsController | ✅ | list / acknowledge / resolve / export |
| WorkOrdersController | ✅ | 全生命周期 (create→assign→start→complete→accept→close) |
| KnowledgeController | ✅ | rules CRUD / pending-rules approve/reject |
| DashboardController | ✅ | stats / OEE |
| TenantsController | ✅ | CRUD / plan-change / freeze/unfreeze |
| AlertRulesController | ❌ | 未覆盖 |
| GatewaysController | ❌ | 未覆盖 |
| AnalysesController | ❌ | 未覆盖 |
| ...其余 19 个 | ❌ | 多数未覆盖 |

### 4.2 Testcontainers 模式

```csharp
// 使用 WebApplicationFactory<Program> + Testcontainers PostgreSQL
public class WorkOrderControllerTests : IClassFixture<IntegrationTestFixture>
{
    [Fact]
    public async Task FullLifecycle_ShouldSucceed()
    {
        // Create
        var createResponse = await _client.PostAsJsonAsync("/api/v1/work-orders", createReq);
        createResponse.StatusCode.Should().Be(HttpStatusCode.Created);
        var wo = await createResponse.Content.ReadFromJsonAsync<WorkOrderDto>();

        // Assign → Start → Complete → Accept → Close
        await _client.PutAsync($"/api/v1/work-orders/{wo!.Id}/assign", assignReq);
        await _client.PutAsync($"/api/v1/work-orders/{wo.Id}/start", null);
        // ... 完整链路验证
    }
}
```

---

## 五、前端单元测试

### 5.1 测试分布

| 层 | 测试文件数 | 覆盖内容 |
|---|-----------|---------|
| **Hooks** | 20 | useSignalR, useAlerts, useWorkOrders, useDevices, useTokenRefresh, useDashboard, useKnowledge, useDataQuality, useNotifications, useOfflineStatus, useIntegration, usePermission, useKnowledgeConflictAndBatch |
| **Stores** | 3 | authStore (setAuth/logout/loadFromStorage), notificationStore (push/markRead/clearAll), realtimeStore |
| **Components** | 10 | SeverityBadge, StatusTimeline, SlaCountdown, OfflineStatusBadge, OfflineSyncPanel, WorkOrderForm, RuleEditDialog, DeviceForm, DeviceStatusBadge, AlertDetailDrawer |
| **Pages** | 1 | PendingApprovalsPage |

### 5.2 测试模式

```typescript
// useSignalR.test.tsx — mock 关键依赖
vi.mock('../../lib/signalr', () => ({
  startConnection: () => mockStart(),
  stopConnection: () => mockStop(),
}));

it('should register handlers after connection', async () => {
  const { result } = renderHook(() => useSignalR(), { wrapper });
  // ...验证 handler 注册
});
```

### 5.3 组件测试缺口

| 组件 | 测试文件 | 状态 |
|------|---------|------|
| SeverityBadge | ✅ SeverityBadge.test.tsx | ✅ |
| StatusTimeline | ✅ StatusTimeline.test.tsx | ✅ |
| SlaCountdown | ✅ SlaCountdown.test.tsx | ✅ |
| AlertDetailDrawer | ❌ | ❌ |
| AnalysisDetail | ❌ | ❌ |
| ConfidenceMeter | ❌ | ❌ |
| TrendChart | ❌ | ❌ |
| PieChart | ❌ | ❌ |
| DataQualityOverview | ❌ | ❌ |
| ModbusTcpConnectionForm | ❌ | ❌ |
| DeviceForm | ✅ | ✅ |
| RuleEditDialog | ✅ | ✅ |
| **组件测试覆盖率** | **10 / 50+** | **~20%** |

---

## 六、E2E 测试详情

### 6.1 Playwright 项目依赖链

```
setup (健康检查+种子数据)
  └── auth (登录/注册) ──→ crud (设备/告警/工单 CRUD)
                            ├── realtime (SignalR/MQTT)
                            ├── advanced (导出/导入/MFA/OEE)
                            ├── auth-extended (Token/RBAC)
                            ├── errors (400/401/403/500)
                            └── edge-cases (SQLi/XSS/并发/离线)
```

### 6.2 安全测试覆盖

| 攻击类型 | E2E 场景 | 期望结果 |
|---------|---------|---------|
| SQL 注入 | POST body 含 "OR 1=1" | 400 + "请求被安全策略拦截" |
| XSS 注入 | POST body 含 "<script>" | 400 + "请求包含不允许的内容" |
| 路径遍历 | URL 含 "../" | 403 + "请求被安全策略拦截" |
| 超长输入 | 100KB payload | 400 (ASP.NET 自动截断) |
| 损坏 Token | 随机字符串 | 401 |
| 速率限制 | 高频请求 (60+/min) | 429 |
| 离线队列 | 断网→操作→恢复→同步 | 操作同步成功 |

### 6.3 实时推送覆盖

| 事件 | E2E 验证方式 |
|------|-------------|
| OnAlertTriggered | 用 MQTT 模拟器发异常遥测 → 检查前端告警列表实时更新 |
| OnWorkOrderCreated | API 创建工单 → 检查 SignalR 推送 |
| OnDeviceStatusChanged | 等待设备超时 → 检查设备列表状态更新 |
| OnGatewayOffline | 停止网关心跳 → 检查网关列表状态 |

### 6.4 关键配置

```typescript
// playwright.config.ts
{
  retries: process.env.CI ? 1 : 0,   // CI 失败重试 1 次防 flaky
  timeout: 60000,                      // 单用例超时 60s
  expect: { timeout: 15000 },         // 断言超时 15s (CI 环境较慢)
  workers: 2,                          // CI 中 2 个 worker
  fullyParallel: false,                // 跨租户隔离不能并行
}
```

---

## 七、K6 压力测试

| 脚本 | 场景 | VUs | 阶段 | 已集成 CI |
|------|------|-----|------|----------|
| k6/login.js | 高并发登录 | 50→100→200 | 3 阶段 (2m each) | ❌ |
| k6/devices.js | 设备 CRUD 吞吐 | 20→50→100 | 3 阶段 | ❌ |
| k6/alerts.js | 告警引擎并发 | 50 固定 | 5m | ❌ |
| k6/telemetry.js | MQTT 高吞吐 | 100 固定 | 5m | ❌ |
| k6/full-workflow.js | 全链路 | 10→30→50 | 3 阶段 | ❌ |
| api-load.js | 综合负载 | 200 VUs | 10m | ❌ |

**建议**：至少将 `login.js` 和 `api-load.js` 加入 CI（不阻断，仅输出基准对比）。

---

## 八、改进路线图

| 优先级 | 改进项 | 工作量 | 影响 | 详情 |
|--------|--------|--------|------|------|
| P1 | 补充集成测试到余下 19 个 Controller | 大 (5天) | 捕获 API 回归 | 按业务重要性排序 |
| P2 | 前端组件测试补充 | 中 (3天) | 提高 50+ 组件质量信心 | 优先 AlertDetailDrawer + TrendChart |
| P2 | 添加代码覆盖率阈值 | 小 (0.5天) | 防覆盖率下降 | vitest + dotnet-coverage |
| P2 | K6 压测集成 CI | 小 (1天) | 建立性能基线 | 先加 login.js 和 api-load.js |
| P3 | E2E 精简 (512→270) | 中 (3天) | 减少 CI 30min→20min | 合并重复场景 |
| P3 | 领域层 AI 管线测试补充 | 中 (2天) | 降级逻辑覆盖 | RootCauseAnalysisEngine 单元测试 |
| P3 | 边缘网关集成测试 | 中 (1天) | 协议兼容性 | Mock OPC UA Server |

---

## 九、评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 后端单测 | ⭐⭐⭐⭐☆ | 835 用例，Mock 使用恰当，xUnit 统一框架 |
| 集成测试 | ⭐⭐⭐☆☆ | 103 仅覆盖 11/30 Controller，缺口大 |
| 前端单测 | ⭐⭐⭐⭐☆ | 321 hooks/stores 覆盖好，组件测试仅 ~20% |
| E2E 覆盖 | ⭐⭐⭐⭐⭐ | 512 用例 + 安全边界，覆盖非常充分 |
| 压测 | ⭐⭐⭐☆☆ | 7 个 K6 脚本已完备但未集成 CI |
| 金字塔平衡 | ⭐⭐⭐☆☆ | E2E 29% 偏高，集成 6% 偏低 |

---

## 关联报告

| 领域 | 报告 |
|------|------|
| 架构总览 | [S01-全系统架构总览图](./S01-全系统架构总览图.md) |
| 代码质量 | [05-代码质量分析](./05-代码质量分析.md) |
| CI/CD 集成 | [08-DevOps与CI_CD分析](./08-DevOps与CI_CD分析.md) |
| 技术债务 | [13-技术债务与改进路线图](./13-技术债务与改进路线图.md) |
| Bug 演进 | [S06-Bug历史与演进故事](./S06-Bug历史与演进故事.md) |

---
*本文档属于 EquipSense 项目评估体系 · 生成日期：2026-06-24 · 版本：v3.1*
